# Settings for the ESP Dev board
FLASH_SIZE = 32
FLASH_MODE = dio
FLASH_SPEED = 80
ESPPORT = "/dev/tty.SLAB_USBtoUART"

# Settings for a Sonoff Basic
# FLASH_SIZE = 8
# FLASH_MODE = dout
# FLASH_SPEED = 40
# ESPPORT = "/dev/tty.usbserial-A50285BI"
